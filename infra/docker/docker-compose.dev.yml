version: '3.8'
services:
  api:
    build: ./backend
    ports:
      - '8000:8000'
    environment:
      - DATABASE_URL=${DATABASE_URL}
    volumes:
      - ./backend/app:/app/app

  agents:
    build: ./agents
    environment:
      - CCS_API_BASE=http://api:8000
    depends_on:
      - api
